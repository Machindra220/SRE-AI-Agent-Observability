# =============================================================
# agent/requirements.txt
# Dependencies for the SRE AI Agent (Phase 19)
#
# Install:
#   pip install -r agent/requirements.txt
# =============================================================

# ── LangGraph + LangChain ─────────────────────────────────────
langgraph==0.2.28          # LangGraph state machine framework
langchain==0.3.1           # LangChain core utilities

# ── LLM + Embeddings — Google Gemini ─────────────────────────
google-generativeai==0.8.3 # Gemini API — used for both LLM and embeddings
                           # LLM: gemini-1.5-flash (diagnoser.py)
                           # Embeddings: text-embedding-004 (embeddings.py)
                           # Free tier, no local model, zero RAM spike

# ── Vector Database ───────────────────────────────────────────
psycopg2-binary==2.9.10    # PostgreSQL driver
pgvector==0.3.2            # pgvector Python adapter

# ── API Server ────────────────────────────────────────────────
fastapi==0.115.0           # FastAPI web framework
uvicorn==0.30.6            # ASGI server to run FastAPI

# ── Observability ─────────────────────────────────────────────
ddtrace==2.11.3            # Datadog APM tracing

# ── Utilities ─────────────────────────────────────────────────
pydantic==2.9.2            # Request/response validation
python-dotenv==1.0.1       # Load .env file for local development