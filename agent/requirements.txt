# =============================================================
# agent/requirements.txt
# Dependencies for the SRE AI Agent (Phase 19)
#
# Install:
#   pip install -r agent/requirements.txt
# =============================================================

# ── LangGraph + LangChain ─────────────────────────────────────
langgraph==0.2.28          # LangGraph state machine framework
langchain==0.3.1           # LangChain core utilities

# ── LLM — Anthropic Claude ───────────────────────────────────
anthropic==0.34.2          # Claude API client (used directly in diagnoser.py)

# ── Embeddings ────────────────────────────────────────────────
sentence-transformers==3.1.1   # all-MiniLM-L6-v2 embedding model

# ── Vector Database ───────────────────────────────────────────
psycopg2-binary==2.9.9     # PostgreSQL driver (binary = no build deps needed)
pgvector==0.3.2            # pgvector Python adapter for vector column type

# ── API Server ────────────────────────────────────────────────
fastapi==0.115.0           # FastAPI web framework
uvicorn==0.30.6            # ASGI server to run FastAPI

# ── Observability ─────────────────────────────────────────────
ddtrace==2.11.3            # Datadog APM tracing

# ── Utilities ─────────────────────────────────────────────────
pydantic==2.9.2            # Request/response validation (used by FastAPI)
python-dotenv==1.0.1       # Load .env file for local development